spring:
  application:
    name: neo
server:
  port: 8080
  servlet:
    context-path: /neo
eureka:
  enabled: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      registerWithEureka: true
      fetchRegistry: true
  instance:
    preferIpAddress: true
